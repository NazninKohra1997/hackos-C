#include<stdio.h>
#include<string.h>

int main(){
    char c[10];
    char s[10000];
    char t[100000];

    //scanf("%s",&c);
    gets(c);
    gets(s);
    gets(t);

    //printf("%s",c);
    puts(c);

    puts(s);

    puts(t);

    return 0;
}
